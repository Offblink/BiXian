"""Bi-Xian (笔仙) - one-line, auditable, thresholdable decisions for agents.

This package is deliberately dependency-free (stdlib only) and targets Python
3.9+ so it can run on whatever machine an agent happens to be on.
"""

__version__ = "0.0.1"
